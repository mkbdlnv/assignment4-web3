<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <link type="text/css" rel="stylesheet" href="/css/style.css">
    <script src="https://kit.fontawesome.com/597061bca4.js" crossorigin="anonymous"></script>
    <title>Home</title>
</head>
<body>
    <%- include('partials/header') %>
    <main class="container-fluid d-flex align-items-center flex-column">
            <div id="profile">
                <div class="row">
                    <form method="post" action="/add-post">
                        <label for="exampleFormControlTextarea1" class="form-label">Write post</label>
                        <textarea class="form-control" name="text" id="exampleFormControlTextarea1" rows="3"></textarea>
                        
                        <button type="submit" class="btn btn-primary">POST</button>
                      </form>
                </div>
            </div>
            <div id="profile">
                <% posts.forEach(post => { %>
                <div class="row">
                    <div class="col d-flex align-items-center">
                        <h3><%= post.name + ' ' + post.lastname %></h3>
                        <% 
                        // Assuming timestamp is passed as 'timestamp' variable
                        const timestamp = new Date(post.created_at); // Convert timestamp to Date object
                        const date = timestamp.toLocaleDateString('en-US'); // Format date
                        const time = timestamp.toLocaleTimeString('en-US'); // Format time
                        %>
                        <i class="fa fa-calendar ms-2" aria-hidden="true"></i><p class="ms-2 pt-3 fst-italic"><%= date + '  ' + time %></i></p>
                    </div>
                    <div class="col d-flex justify-content-end">   <div id="avatar" style="width: 50px; height: 50px;"><img src="<%= post.img_path %>" alt="Avatar"></div></div>
                </div>
                <div class="row"><p><%= post.text %></p></div>
                <% }); %>
            </div>
    </main>

</body>
</html>
